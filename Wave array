// In this problem we sort the array after that we swap the every second element and iterate the loop after every second element.and swap them
 public int[] wave(int[] A) {
        int temp;
        Arrays.sort(A);
        for(int i=0;i<A.length-1;i=i+2){
                temp = A[i];
                A[i] = A[i+1];
                A[i+1] =   temp;
            }
            return A;
    }
}
