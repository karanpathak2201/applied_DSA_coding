// In this problem we can iterate over the two loop and then we can check if the sum of two number is equal to target sum.and the value of i does not
equal to the value of j
public int[] twoSum(int[] nums, int target) {
        int[] ans = new int[2];
        for(int i=0;i<nums.length-1;i++){
            for(int j =1; j<nums.length;j++){
                if(nums[i] + nums[j] == target & i!=j)
                    return new int[]{i,j};
}
        }
        return new int[2];
    }
}
