// In this question we check if the  previous element is greater than its next element than we substract that next element from previous element
public int maxProfit(int[] prices) {
        int profit =0;
        for(int i=0;i<prices.length-1;i++){
            if(prices[i] < prices[i+1])
                profit += prices[i+1]  - prices[i];
            }
        return profit;
    }
}
